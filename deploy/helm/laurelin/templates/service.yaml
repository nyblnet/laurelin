apiVersion: v1
kind: Service
metadata:
  name: {{ include "laurelin.fullname" . }}
  labels:
    {{- include "laurelin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "laurelin.selectorLabels" . | nindent 4 }}
