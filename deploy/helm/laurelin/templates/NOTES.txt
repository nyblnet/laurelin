Laurelin {{ .Chart.AppVersion }} is deploying.

1. Wait for the pod(s) to become ready:
     kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }} -w

2. Reach the UI:
{{- if .Values.ingress.enabled }}
     https://{{ (index .Values.ingress.hosts 0).host }}/
{{- else }}
     kubectl port-forward svc/{{ include "laurelin.fullname" . }} 8787:{{ .Values.service.port }}
     open http://localhost:8787
{{- end }}

3. First run: open the UI and create the server administrator account.

{{- if not .Values.controlDatabaseUrl }}
{{- if not .Values.existingControlDbSecret }}

WARNING: no PostgreSQL control plane configured (controlDatabaseUrl /
existingControlDbSecret). Identity + the workspace registry will use SQLite on
the data volume — fine for a single replica, but set a Postgres URL before
scaling to multiple replicas.
{{- end }}
{{- end }}
{{- if gt (int .Values.replicaCount) 1 }}
{{- if and (not .Values.controlDatabaseUrl) (not .Values.existingControlDbSecret) }}

WARNING: replicaCount is {{ .Values.replicaCount }} with no PostgreSQL control
plane. Workspaces then keep metadata in per-workspace SQLite files, which
CANNOT be shared safely across hosts — a ReadWriteMany volume does not make
this safe, it is what makes it dangerous.

Set controlDatabaseUrl (or existingControlDbSecret), or run replicaCount=1.
{{- else if not (index .Values.env "LAURELIN_DATA_URI") }}

NOTE: replicaCount is {{ .Values.replicaCount }} with a PostgreSQL control
plane — metadata is shared safely. Dataset Parquet still lives on the volume,
so every replica must mount the same ReadWriteMany claim
(persistence.accessMode is "{{ .Values.persistence.accessMode }}").

To drop the shared volume entirely, set env.LAURELIN_DATA_URI to an object
store (s3://…, gs://…, abfs://…) and persistence.enabled=false.
{{- end }}
{{- end }}
